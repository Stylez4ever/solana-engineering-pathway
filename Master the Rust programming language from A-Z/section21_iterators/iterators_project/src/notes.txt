To iterate
- means to perform repeatedly
Iteration
- is repeating the same operation on a sequence of items, one item at a time

The iter method 
- will create an iterator that yields immutable references to the 
collections's

The into_iter method
- take ownership of the original collection's data

The iter_mut method
- wil create an iterator that yields mutable references to the collection's elements

The for_each method
- applies a consistent operation to every iterator element. it accepts a closure

An adapter method
- is one that transforms an iterator into another iterator based on some logic


The collect method
- exhausts the iterator and gathers the resulting values in a new collection type 


 OWNERSHIP
 for value in collection
 for value in collection.into-iter()


 IMMUTABLE REFERENCES
 for value in &collection
 for value in collection.iter()


 MUTABLE REFERENCES
 for value in &mut collection
 for value in collection.iter_mut()


The filter method
- extracts a subset of values that satisfy a condition. Pass a closure that returns true
for the elements to keep and false for the elements to exclude 

The copied method
- this is a useful when you have an iterator over &T, but you need an iterator over The

The find method
- will collect the first element for which the closure returns true 

The all method
- is a consuming adaptor for iterator that tests whether every element of an iterator 
satisfies a given predicate (a closure that returns true or false)

The any method
- is a consuming adaptor defined on the Iterator trait that checks if at least one element
in an iterator satisfies a given predicate (a closure that returns true or false)

The map method
- is a functional programming tool used to transform elements within a collection (like a Vector, Array ot Iterator)
or to transform the inner value of containers types Option or Result

The for_each method
- an iterator consumer (or terminal operation) that applies a given clousre to every elemenof an iterator
it is used to process each item in a sequence, usually for side effects (such as printing or modifying data), and
consumes the iterator in the process

The filter_map method
- both filters and transforms a subset of elements from an iterator

The enumerate adapter
- transforms an iterator so that it yields the index position along with the current element

The partition method
- groups and returns the value for which the closure returns true and for which the closure returns false

The zip method 
- combines two iterators togeher based on their elements having the same index position

The fold method
- exhausts an iterator to build up and produce a single value at the end of iteration

The reduce method
- is similar to fold but it supplies the iterator's frist element as the starter value
- it also returns an Option enum to account for the possibility of an empty iterator

